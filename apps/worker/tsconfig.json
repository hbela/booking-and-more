{
  "extends": "@bam/tsconfig/library.json",
  "//": "Editor + ESLint project. Emitting is done by tsconfig.build.json.",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["src/**/*.ts", "*.config.ts"],
  "exclude": ["node_modules", "dist"]
}
